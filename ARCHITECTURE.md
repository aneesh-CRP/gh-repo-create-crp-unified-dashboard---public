# CRP Unified Intelligence Dashboard — Architecture & Session Notes

> Last updated: March 17, 2026
> For future Claude sessions or developers picking this up

## Overview

Single-page dashboard (`index.html`, ~13K+ lines) serving CRP clinical research operations data. Deployed on two platforms in parallel:

- **GitHub Pages** (primary, fully working): https://aneesh-crp.github.io/gh-repo-create-crp-unified-dashboard---public/
- **Google Apps Script** (SSO-protected): deployed as a web app restricted to `phillyresearch.com` domain — **not yet fully working** (see Known Issues)

## Key Files

| File | Purpose |
|------|---------|
| `index.html` | Main dashboard (~13K+ lines, all HTML/CSS/JS in one file) |
| `dashboard.js` | Extracted JS (generated by build script, also served on GitHub Pages) |
| `styles.css` | Extracted CSS (generated by build script) |
| `fallback-data.json` | Sample/fallback data (~295KB, extracted from index.html) |
| `sw.js` | Service worker for offline support |
| `coord-history.json` | Persistent coordinator visit history (committed to repo) |
| `tests/run.js` | Unit tests (no external dependencies) |
| `appscript/WebApp.gs` | Apps Script server: doGet, proxyFetch, getDashboardJSChunk |
| `appscript/DashboardJS_N.gs` | JS chunks as escaped string constants (generated by build script) |
| `appscript/appsscript.json` | Apps Script manifest |
| `scripts/build-appscript.py` | Splits index.html → Dashboard.html + .gs JS chunks + styles.css (uses BUILD marker comments) |
| `deploy-appscript.sh` | Local deploy helper script |
| `.github/workflows/deploy.yml` | Auto-deploy to GitHub Pages on push to main |
| `.github/workflows/deploy-appscript.yml` | Auto-deploy to Apps Script on push to main |

## Apps Script Architecture

The dashboard is too large (~970KB) for Apps Script's HtmlService, so we use an **async chunked loading** pattern:

1. `doGet()` serves `Dashboard.html` (~263KB, HTML/CSS shell only) via `createHtmlOutputFromFile`
2. Client-side loader calls `google.script.run.getDashboardJSChunk(0..3)` in parallel
3. JS chunks are stored as **string constants in `.gs` files** (`_JS_CHUNK_0`, `_JS_CHUNK_1`, etc.) — this bypasses HtmlService entirely, which corrupts JS containing HTML-like patterns (innerHTML, template literals with HTML tags, etc.)
4. Each chunk is <200KB (under google.script.run's ~256KB return limit)
5. Once all chunks arrive, they're concatenated and injected as a `<script>` element
6. The init function checks `document.readyState` to handle the fact that `DOMContentLoaded` has already fired by the time injected JS executes
7. Data fetching routes through `google.script.run.proxyFetch(url)` → server-side `UrlFetchApp` to bypass iframe CSP

**Script ID:** `15ZDwJzbYqFVtVc5e4TGl7yEWZFVKq2pQl-RyOKw8vsVnAGBqWrkSO19K`

**Web App Deployment ID:** `AKfycbw1xcyJDv4f2BIaGoFxLgRM86RvWffMyW4QKPbTk8PPEd73cWP8zr9njDooGlNLtWoP`

**Web App URL:** `https://script.google.com/a/macros/phillyresearch.com/s/AKfycbw1xcyJDv4f2BIaGoFxLgRM86RvWffMyW4QKPbTk8PPEd73cWP8zr9njDooGlNLtWoP/exec`

### Why .gs files instead of .html for chunks

Previous attempts stored JS chunks as `.html` files and read them via `HtmlService.createHtmlOutputFromFile().getContent()`. This failed because HtmlService processes the content as HTML, corrupting JS that contains HTML-like patterns (which this dashboard has extensively — innerHTML assignments, template literals with `<div>`, `<table>`, etc.). The `.gs` approach stores the JS as a JSON-escaped string variable, completely bypassing HTML processing.

## Deploy Pipeline

**GitHub Pages:** Automatic via `.github/workflows/deploy.yml` on push to main.
- Scoped to only publish `index.html`, `dashboard.js`, `coord-history.json`, `fallback-data.json`, `sw.js`, and `styles.css` (not the entire repo root)
- This prevents exposing `.clasp.json`, `.gs` files, `push.command`, and other internal files on the public Pages site

**Apps Script:** Automatic via `.github/workflows/deploy-appscript.yml`:
1. Triggered on push to main (paths: index.html, dashboard.js, appscript/**, scripts/**, the workflow itself)
2. Runs `python3 scripts/build-appscript.py` to generate Dashboard.html + `.gs` JS chunks
3. Validates clasp credentials (JSON parse check before push)
4. `clasp push --force` uploads to Apps Script project
5. `clasp deploy -i <deployment_id>` updates the existing web app deployment (not creating new ones)

**Secret required:** `CLASPRC_JSON` in GitHub repo settings (OAuth credentials from `~/.clasprc.json`). Must be set via `gh secret set CLASPRC_JSON < ~/.clasprc.json` — the JSON content is written using `printenv` to avoid shell escaping issues.

## Tabs

- **Overview** — KPI banners, enrollment pipeline, trends
- **Studies** — merged study table + CRIO Recruitment Intelligence + Scheduling Gaps + Re-enrollment Opportunities
- **Schedule** — At-Risk Patients, Medical Records, visit table, cancellations, coordinator goals, Prioritized Action Plan
- **Referrals** — ClickUp pipeline, campaigns, Facebook leads, Patient Pipeline by Office, Contact Quality Alerts
- **Admin** — trends, data export (PIN-protected)
- **Finance tabs** (6) — PIN-protected

> **Note:** "Actions" tab was eliminated March 17; all sections redistributed into Studies/Schedule/Referrals.

## Data Sources

All data is fetched from Google Sheets published as CSV. On GitHub Pages, standard `fetch()` is used. On Apps Script, fetches route through `google.script.run.proxyFetch()` using the `IS_APPS_SCRIPT` detection flag.

### CRIO Integration

- **API**: `api.clinicalresearch.io`, Site ID 1679 (PHL), Client ID 1329
- **Pipeline**: CRIO API → Apps Script `syncCrioData()` → Google Sheets (gid=932189572 studies, gid=941302257 subjects) → Published CSV → Dashboard
- **PRESCREEN_OVERRIDES**: 8 entries mapping pre-screening study_keys to protocol study_keys
- **INDICATION_ALIASES**: 17 entries normalizing fragmented CRIO indication strings into unified therapeutic areas
- **Refresh**: 15-minute auto-refresh via `autoRefreshAll()`, CRIO data fetched with `_crioFetchInFlight` guard (try/finally)
- **Features**: Win-back opportunities, qualified-not-scheduled, pre-screening outreach, enrolled-no-visit alerts, re-enrollment opportunities by indication

### Data Refresh Architecture

- `refreshData()` / `autoRefreshAll()` — guarded by `_refreshInFlight`
- Phase 1: CTMS data (fetchCSV → processLiveData → DATA → renderAll)
- Phase 2: Finance (staggered)
- Phase 3: Supplemental (Patient DB, Facebook CRM, CRIO studies — staggered)
- `fetchCrioStudies()` — guarded by `_crioFetchInFlight` with try/finally
- `manualRefresh()` — wraps refreshData with button disable/re-enable

## localStorage Keys

- `crp_coord_snapshot_v1` — Coordinator visit data snapshot
- `crp_dismissed_actions_v1` — Dismissed action items (60-day auto-prune)
- `crp_coll_[invoice]` — Invoice collection status/notes
- `crp_clickup_token` — ClickUp API token (user-entered)
- `crp_confirmed_visits` — Confirmed visit checkboxes
- `crp_fin_auth` (sessionStorage) — Finance PIN auth flag
- `crp_theme` — Dark/light mode preference
- `crp_audit_log` — Dashboard audit log (auto-pruned to 500 entries)

## Coordinator Visit History

Three-layer merge system (highest count wins):
1. **coord-history.json** (permanent baseline, committed to repo)
2. **localStorage snapshot** (recent browser-cached data)
3. **Live CRIO data** (fetched at runtime)

Export button (⬇) in Coordinator Visit Snapshot header downloads merged history as JSON.

## New Machine Setup

1. Clone repo
2. `npm install -g @google/clasp`
3. `clasp login` (opens browser for Google OAuth — use `@phillyresearch.com` account)
4. Copy `.clasp.json` to `appscript/` with the Script ID above
5. Update `CLASPRC_JSON` secret: `gh secret set CLASPRC_JSON --repo aneesh-CRP/gh-repo-create-crp-unified-dashboard---public < ~/.clasprc.json`
6. Everything else auto-deploys via GitHub Actions

## Known Issues / Status (as of March 18, 2026)

- **GitHub Pages dashboard:** ✅ Fully working, verified live
- **Apps Script dashboard:** ❌ Not working reliably. **Recommendation:** Embed the GitHub Pages URL in a Google Sites page restricted to `phillyresearch.com` instead.
- **GitHub Actions:** ✅ Both workflows (Pages + Apps Script) passing
- **GitHub Pages deploy scoping:** ✅ Only dashboard files are published

## Outstanding Issues

- **~70 console.log statements** could be trimmed for production
- **~40 innerHTML assignments** in finance views still lack escapeHTML wrapping
- **Detail modal** reattaches sort handlers on every open
- **Re-enrollment** is study-level only (no patient-level cross-study tracking from CRIO)
- **3 PNJ studies** (20230222, N1T-MC-MALO, J3F-MC-EZCC) always show stale fallback data (not in CRIO PHL site)
- **PHI in fallback-data.json**: Should be fetched at runtime behind authentication
- **SSRF in proxyFetch**: `appscript/WebApp.gs` accepts any URL with zero validation
- **Client-side PIN auth**: Bypassable; all finance data is in page source
- **stageColors defined 7 times**: Inconsistent values across functions
- **Duplicate functions**: `sortTable`/`sortDetailTable`, `drawRevChart`/`drawRevChartOn`
- **Stale files**: Root-level `WebApp.gs` and `finance_consolidator.gs` are outdated duplicates

## Session History

### March 12, 2026 (morning session)
1. Persistent coordinator visit history (coord-history.json + 3-layer merge + export)
2. Apps Script web app deployment with Google Workspace SSO
3. GitHub Actions auto-deploy pipeline for Apps Script
4. Dismiss/complete buttons on Actions tab (localStorage-persisted)
5. Server-side proxy for Apps Script data fetching (proxyFetch via UrlFetchApp)
6. Async chunked JS loading to bypass HtmlService + google.script.run size limits

### March 12, 2026 (evening session)
1. Full code review (security, performance, code quality)
2. Fixed hardcoded dates → `new Date()` in both `index.html` and `dashboard.js`
3. Fixed invalid CSS `flex-wrap: gap` → `flex-wrap: wrap`
4. Scoped GitHub Pages deploy to only serve dashboard files
5. Fixed Apps Script deploy pipeline (credential writing, deployment ID targeting)
6. Updated CLASPRC_JSON secret from new machine
7. Switched JS chunks from `.html` to `.gs` string constants to bypass HtmlService corruption
8. Fixed DOMContentLoaded timing for async chunk injection
9. Apps Script dashboard still not reliably working — recommended Google Sites embed as alternative

### March 12, 2026 (14-enhancement session)
Implemented all 5 phases of the enhancement plan:

**Phase 1 — Quick Wins:**
1. Deep-link tabs via URL hash (#17) — `history.replaceState` in switchTab, hashchange listener, hash-aware init
2. Data freshness indicator (#7) — "Updated X min ago" badge with color coding (green/yellow/red)
3. Error toast notifications (#8) — slide-in toasts for fetch errors, exports, data status
4. Print-friendly styles (#18) — enhanced @media print, beforeprint header, active-view-only printing

**Phase 2 — Medium Features:**
5. Dark mode toggle (#1) — `[data-theme="dark"]` CSS variables, localStorage persistence, Chart.js theme update
6. Mobile-responsive layout (#2) — hamburger menu at <=800px, auto-close on tab select
7. Global search/filter (#3) — debounced search across patients/studies/coordinators, respects PHI masking
8. Export to PDF/Excel (#4) — window.print() for PDF, CSV blob generation for table data

**Phase 3 — Data Layer:**
9. Sanitize innerHTML (#14) — `escapeHTML()` utility, applied to slink() and key rendering functions
10. Move SAMPLE data to external file (#5) — `fallback-data.json` (295KB), async loader, minimal inline skeleton
11. Audit log (#12) — localStorage-persisted event log (tab views, exports, finance unlock), read-only admin viewer

**Phase 4 — Infrastructure:**
12. Offline mode with service worker (#6) — `sw.js` with network-first for data, cache-first for static, offline banner
13. Basic tests (#15) — `tests/run.js` with 16 tests for escapeHTML, parseCSV, data validation, file existence

**Phase 5 — Structural Refactor:**
14. Break up the monolith (#13) — BUILD marker comments, `styles.css` extraction, updated build script to use markers instead of line-number heuristics

### March 13, 2026
1. Clickable popups on all KPI numbers (Overview, Schedule, Risk, Contact Quality, Referral, Trends)
2. Rescheduled visits excluded from cancel counts (cross-references cancel list against upcoming visits)
3. Rescheduled visits as separate KPI category with confirmed vs pending split

### March 17, 2026
**CRIO Recruitment Intelligence — Actionable Rewrite:**
1. Win-Back Opportunities — cumulative V1 no-shows per study
2. Recent Cancels Not Rebooked — from pending reschedules
3. Qualified Not Yet Scheduled — SCREENING status per protocol study
4. Pre-Screening Outreach Needed — PREQUALIFIED per pre-screening study
5. Enrolled with No Upcoming Visit — enrolled vs calendar gap
6. Re-Enrollment Opportunities — SCREEN_FAIL + COMPLETED + DISCONTINUED grouped by normalized indication

**Actions Tab Eliminated:**
- → Studies: CRIO Intelligence, Scheduling Gaps
- → Schedule: At-Risk Patients, Medical Records, Prioritized Action Plan + Dormant Studies
- → Referrals: Contact Quality Alerts
- Removed: Data Quality Notes (stale hardcoded)

**Data Accuracy Fixes:**
- INDICATION_ALIASES (17 entries): normalizes fragmented CRIO indication strings
- PRESCREEN_OVERRIDES (8 entries): maps pre-screening study_keys to protocol study_keys
- KPI pre-screening exclusion: enrollment KPIs exclude pre-screening + placeholder subjects
- SKIP_PROTO: placeholder studies excluded from KPI totals
- study_url always overridden from CRIO study_key (PHL site)

**Refresh Safety:**
- `_crioFetchInFlight` guard with try/finally (prevented permanent lock on empty data)
- CRIO data refreshes on both manual and auto-refresh cycles
- `buildSchedulingGapAlerts()` wired up (was dead code)
- `manualRefresh()` wrapper with button disable during refresh

**Chart.js Fix:**
- `mkChart()` now adds explicit `type: 'category'` / `type: 'linear'` to all scale configs, suppressing Chart.js 4.x "Ignoring resolver" console warnings

**Referral/Leads Matching Fix:**
- `REFERRAL_STUDY_MAP` (7 entries): Maps referral CSV nicknames (Ocean(a), PREVENT-HF, Sjogren's, etc.) to protocol numbers
- `REFERRAL_STUDY_SKIP` + `isReferralStudyGarbage()`: Filters garbage values (doctor names, dates, blanks) from study column
- Fixed empty-string bug (`includes('')` = true), removed bidirectional fuzzy matching, added min-length guard
- Aliases pre-indexed into `_referralByStudy` Map for O(1) lookups

**Commits:** `3ad09e3`, `0ba34b2`, `2cecc49`, `3bb1809`, `ab9f441`, `ce39eb7`, `4273214`, `cba8bae`, `2c8e5a2`, `88e3563`

### March 18, 2026
**Site Assignment Fix — PHL/PNJ Misclassification:**

The upcoming visits CSV (LIVE_URL1) has no `Site Name` column. Previously, site was derived from URL strings or hardcoded coordinator names, causing all PNJ visits to show as PHL and the PNJ filter to return zero results.

**Root cause fixes:**
1. **Site Name backfill at parse time** — `processLiveData()` now injects `Site Name` into every raw CSV row using `PENNINGTON_KEYS` + `siteSlug()` right after parsing, before any downstream code runs. Single injection point means all views get correct site data automatically.
2. **buildScheduleTable** — uses `v.site` (actual Site Name) instead of `study_url.indexOf('pennington')` heuristic
3. **schedFilter** — `data-site` attribute now stores full site name ("Pennington, NJ") so `site.includes('Penn')` works correctly
4. **Cancel chart click handler** — uses `r.site` instead of hardcoded coordinator-name-to-site proxy
5. **Overview enrollment pipeline** — uses `v.site` instead of `patient_url.includes('pennington')`
6. **Site Name added to EXPECTED_COLUMNS.CRIO** — warns if column appears in future CSV

**Site column added to popups:** Cancellations, Rescheduled Visits (confirmed + pending)

**Coordinator config updated:**
- `COORDINATORS`: Added Ema Gunic, Vlado Draganic, Gabrijela Ateljevic, Ana Lambic, Jana Milankovic
- `SITES.PHL.coordinators`: Added Ana Lambic, Jana Milankovic
- `SITES.PNJ.coordinators`: Added Ema Gunic, Vlado Draganic, Gabrijela Ateljevic
- Coordinator workload views now use `CRP_CONFIG.COORDINATORS` instead of hardcoded 5-name lists

**Schedule tab layout:** Moved upcoming visits table directly under KPI row (was below Medical Records)

**Data verification (live CSV):**
- Upcoming visits: 256 active, 23 PNJ + 233 PHL
- PENNINGTON_KEYS confirmed against cancellation CSV (has authoritative Site Name): 161619, 167755, 167794, 172389 all verified "Pennington, NJ"
- 162446: stale (no data), harmless
- SJS study (150548): confirmed PHL despite PNJ coordinator — correct classification
- D6973C00001 has two study keys: 161619 (PNJ) and 161620 (PHL) — both correctly resolved

**Commits:** `f04d516`, `c3f4baf`, `ed91b31`, `9eebc5f`, `4f99eca`, `574edd7`, `377bf22`
