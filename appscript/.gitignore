# Don't commit the actual clasp config (contains script ID)
.clasp.json

# Don't commit generated files (built from index.html by build-appscript.py)
Dashboard.html
DashboardJS*.html

# QuickBooks credentials — never commit with real keys
QuickBooksSync.gs
